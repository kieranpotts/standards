= Logging and monitoring data

Logs and monitoring data SHOULD be persisted in separate storage from an
application's primary data stores (for state and session data).

Personally identifiable information (PII) MUST NOT be sent to log output.
