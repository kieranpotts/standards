= Security

Access keys and other secrets SHOULD NOT be stored within Docker containers.
This would allow anyone with access to an image to inspect the layers and view
the credentials.

If the containerized application requires credentials such as database
passwords, these SHOULD be mounted at runtime.

See also *link:../052/README.adoc[TS-52: Security and Secrets Management]*.
