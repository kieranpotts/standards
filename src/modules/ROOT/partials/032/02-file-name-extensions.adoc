= File name extensions

Bash file names may have the `.sh` extension or no extension. Bash file names MUST NOT have other extensions such as
`.bash`. Instead, use the shebang (see below) to identify the target interpreter.
