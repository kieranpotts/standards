= References

* Learn JSON Schema (n.d.).
  https://www.learnjsonschema.com/2020-12/[_2020-12 keyword reference_]. —
  The source for the Validation, Applicator, Meta-Data (Annotations), and
  Content vocabulary keyword definitions in <<Validation keywords>>,
  <<Applicator keywords>>, <<Annotations>>, and <<Content keywords>>.

* JSON Schema (n.d.).
  https://json-schema.org/understanding-json-schema/reference/conditionals[_Applying Subschemas Conditionally_].
  — The source for the `if`/`then`/`else` and `dependentRequired`/
  `dependentSchemas` guidance in <<Conditional validation>>.

* JSON Schema (n.d.).
  https://json-schema.org/understanding-json-schema/reference/format[_Format_].
  — The source for the annotation-vs-assertion distinction for `format` in
  <<Format>>.
