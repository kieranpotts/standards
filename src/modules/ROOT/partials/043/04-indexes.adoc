= Indexes

If you expect your table to ever be more than a few rows, you SHOULD put indexes on it.

Try to make indexes match the most common queries.

Indexes work like nested dictionaries, so make sure to put the highest cardinality field first, otherwise each
index lookup will have to scan all uses of `type` before finding the one with the right `email`.


