This this technical standard covers best practices for designing the schema for
JSON Web Tokens (JWTs), which are used for the purpose of stateless
authentication and authorization.

// The goal is creating tokens that are secure, contain sufficient information
for authorization decisions, but remain lightweight and don't expose sensitive
data unnecessarily.
