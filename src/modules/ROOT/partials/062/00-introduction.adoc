This technical standard covers best practices for writing `Makefile`s, using
{link-gnu-make}[GNU Make] as the reference implementation.

Makefiles are commonly used as a language-agnostic task runner – a thin, discoverable interface over project scripts,
builds, and other repeated commands.

It is RECOMMENDED that all code repositories that include scripts and run commands for automating development and
operations processes include a Makefile to encapsulate all the available scripts centrally.

See also:

* xref:031.adoc[*TS-31*] and xref:032.adoc[*TS-32*] for guidance on the shell
  scripts and recipes invoked from within Makefile targets.

* xref:016.adoc[*TS-16*] for general guidance on designing CLIs.

* xref:058.adoc[*TS-58*] for guidance on the container build and run commands that Makefile targets often wrap.
