= Horizontal rules

Use four single-quotes (`''''`) on their own line to insert a horizontal rule. This is typically used to create
thematic breaks between paragraphs, as an alternative to using headings.

```
''''
```
